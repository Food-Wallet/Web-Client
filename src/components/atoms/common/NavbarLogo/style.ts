import styled from "styled-components";

export const NavbarLogoWrapper = styled.div`
    display: flex;
    margin-left: 10px;
    align-items: center;
    cursor: pointer;

    .logo-img {
        height: 40px;
    }
`;